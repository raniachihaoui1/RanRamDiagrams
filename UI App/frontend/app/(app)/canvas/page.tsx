import { PagePlaceholder } from "@/components/shell/PagePlaceholder";

export default function CanvasPage() {
  return (
    <PagePlaceholder title="Canvas" phase="Phase 5">
      The mini-photoshop — layers, brushes, shapes and AI blend — is coming soon.
    </PagePlaceholder>
  );
}
