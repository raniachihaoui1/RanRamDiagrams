import { PagePlaceholder } from "@/components/shell/PagePlaceholder";

export default function GeneratePage() {
  return (
    <PagePlaceholder title="Image Generator" phase="Phase 3">
      Prompt bar with model, LoRA, dimensions, reference and count chips is coming
      next.
    </PagePlaceholder>
  );
}
