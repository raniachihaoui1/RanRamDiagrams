import { PagePlaceholder } from "@/components/shell/PagePlaceholder";

export default function TrainPage() {
  return (
    <PagePlaceholder title="Train LoRA" phase="Phase 5">
      Configure a training run from your dataset and watch its logs. Wiring to the
      x-flux pipeline comes later.
    </PagePlaceholder>
  );
}
